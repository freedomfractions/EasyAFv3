<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:mapVM="clr-namespace:EasyAF.Modules.Map.ViewModels;assembly=EasyAF.Modules.Map"
                    xmlns:mapView="clr-namespace:EasyAF.Modules.Map.Views;assembly=EasyAF.Modules.Map"
                    xmlns:projectVM="clr-namespace:EasyAF.Modules.Project.ViewModels;assembly=EasyAF.Modules.Project"
                    xmlns:projectView="clr-namespace:EasyAF.Modules.Project.Views;assembly=EasyAF.Modules.Project"
                    xmlns:specVM="clr-namespace:EasyAF.Modules.Spec.ViewModels;assembly=EasyAF.Modules.Spec"
                    xmlns:specView="clr-namespace:EasyAF.Modules.Spec.Views;assembly=EasyAF.Modules.Spec"
                    xmlns:shellVM="clr-namespace:EasyAF.Shell.ViewModels"
                    xmlns:shellView="clr-namespace:EasyAF.Shell.Views">
    
    <!-- CROSS-MODULE EDIT: 2025-01-15 Map Module View Hosting -->
    <!-- Modified for: Register DataTemplate to display MapDocumentViewModel with MapDocumentView -->
    <!-- Related modules: Map (MapDocumentViewModel, MapDocumentView) -->
    <!-- Rollback instructions: Remove this file and its reference from App.xaml -->
    
    <!-- Map Module Document View Template -->
    <DataTemplate DataType="{x:Type mapVM:MapDocumentViewModel}">
        <mapView:MapDocumentView />
    </DataTemplate>

    <!-- CROSS-MODULE EDIT: 2025-01-16 Map Module Settings Feature - Step 8 -->
    <!-- Modified for: Register DataTemplate for Map module settings view -->
    <!-- Related modules: Map (MapModuleSettingsViewModel, MapModuleSettingsView) -->
    <!-- Rollback instructions: Remove this DataTemplate -->
    
    <!-- Map Module Settings View Template -->
    <DataTemplate DataType="{x:Type mapVM:MapModuleSettingsViewModel}">
        <mapView:MapModuleSettingsView />
    </DataTemplate>

    <!-- CROSS-MODULE EDIT: 2025-01-20 Task 20 - Project Module View Hosting -->
    <!-- Modified for: Register DataTemplate to display ProjectDocumentViewModel with ProjectDocumentView -->
    <!-- Related modules: Project (ProjectDocumentViewModel, ProjectDocumentView) -->
    <!-- Rollback instructions: Remove this DataTemplate and xmlns:projectVM/projectView declarations -->
    
    <!-- Project Module Document View Template -->
    <DataTemplate DataType="{x:Type projectVM:ProjectDocumentViewModel}">
        <projectView:ProjectDocumentView />
    </DataTemplate>
    
    <!-- CROSS-MODULE EDIT: 2025-01-27 Project Module Settings -->
    <!-- Modified for: Register DataTemplate for Project module settings view -->
    <!-- Related modules: Project (ProjectModuleSettingsViewModel, ProjectModuleSettingsView) -->
    <!-- Rollback instructions: Remove this DataTemplate -->
    
    <!-- Project Module Settings View Template -->
    <DataTemplate DataType="{x:Type projectVM:ProjectModuleSettingsViewModel}">
        <projectView:ProjectModuleSettingsView />
    </DataTemplate>
    
    <!-- CROSS-MODULE EDIT: 2025-11-27 Task 25 - Spec Module View Hosting -->
    <!-- Modified for: Register DataTemplate to display SpecDocumentViewModel with SpecDocumentView -->
    <!-- Related modules: Spec (SpecDocumentViewModel, SpecDocumentView) -->
    <!-- Rollback instructions: Remove this DataTemplate and xmlns:specVM/specView declarations -->
    
    <!-- Spec Module Document View Template -->
    <DataTemplate DataType="{x:Type specVM:SpecDocumentViewModel}">
        <specView:SpecDocumentView />
    </DataTemplate>
    
    <!-- CROSS-MODULE EDIT: 2025-11-28 Task 26 - Spec Module Tab Content Views -->
    <!-- Modified for: Register DataTemplates for Spec module tab content (Setup + Table Editor) -->
    <!-- Related modules: Spec (SpecSetupViewModel, SpecSetupView, TableEditorViewModel, TableEditorView) -->
    <!-- Rollback instructions: Remove these two DataTemplates -->
    
    <!-- Spec Module Setup Tab View Template -->
    <DataTemplate DataType="{x:Type specVM:SpecSetupViewModel}">
        <specView:SpecSetupView />
    </DataTemplate>
    
    <!-- Spec Module Table Editor View Template -->
    <DataTemplate DataType="{x:Type specVM:TableEditorViewModel}">
        <specView:TableEditorView />
    </DataTemplate>
    
    <!-- Global Data Types Settings View Template -->
    <DataTemplate DataType="{x:Type shellVM:DataTypesSettingsViewModel}">
        <shellView:DataTypesSettingsView />
    </DataTemplate>
    
</ResourceDictionary>
