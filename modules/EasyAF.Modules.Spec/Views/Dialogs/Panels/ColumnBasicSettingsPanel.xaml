<UserControl x:Class="EasyAF.Modules.Spec.Views.Dialogs.Panels.ColumnBasicSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    
    <UserControl.Resources>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Margin" Value="0,8,0,4"/>
        </Style>
    </UserControl.Resources>
    
    <StackPanel>
        <TextBlock Text="BASIC SETTINGS" Style="{StaticResource SectionHeaderStyle}"/>
        
        <TextBlock Text="Column Header:" 
                   Margin="0,4,0,2" 
                   Foreground="{DynamicResource TextPrimaryBrush}"/>
        <TextBox Text="{Binding ColumnHeader, UpdateSourceTrigger=PropertyChanged}"
                 Background="{DynamicResource ControlBackgroundBrush}"
                 Foreground="{DynamicResource TextPrimaryBrush}"
                 BorderBrush="{DynamicResource ControlBorderBrush}"
                 Padding="6,4"
                 Margin="0,0,0,8"/>
        
        <Grid Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="Width (%):" 
                           Margin="0,0,0,2" 
                           Foreground="{DynamicResource TextPrimaryBrush}"/>
                <TextBox Text="{Binding WidthPercent, UpdateSourceTrigger=PropertyChanged}"
                         Background="{DynamicResource ControlBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource ControlBorderBrush}"
                         Padding="6,4"/>
            </StackPanel>
            
            <CheckBox Grid.Column="2"
                      Content="Merge Vertically"
                      IsChecked="{Binding MergeVertically}"
                      VerticalAlignment="Bottom"
                      Foreground="{DynamicResource TextPrimaryBrush}"
                      ToolTip="Merge identical adjacent cells (useful for ID columns)"/>
        </Grid>
    </StackPanel>
</UserControl>
