<Window x:Class="EasyAF.Modules.Project.Views.ImportDataDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:EasyAF.Modules.Project.Converters"
        mc:Ignorable="d"
        Title="Import Data"
        Width="700" Height="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource PrimaryBackgroundBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        ResizeMode="CanResize"
        ShowInTaskbar="False">
    
    <Window.Resources>
        <conv:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>
        <conv:GreaterThanZeroConverter x:Key="GreaterThanZeroConverter"/>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="Import Data from File"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <!-- Step 1: File Selection -->
        <GroupBox Grid.Row="1" Header="Step 1: Select Source File" Margin="0,0,0,12">
            <StackPanel Margin="8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0"
                             Text="{Binding SelectedFilePath, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True"
                             Background="{DynamicResource ControlBackgroundBrush}"
                             Padding="8,6"
                             Margin="0,0,8,0"
                             VerticalContentAlignment="Center"/>

                    <Button Grid.Column="1"
                            Content="Browse..."
                            Command="{Binding BrowseFileCommand}"
                            Style="{StaticResource BaseButtonStyle}"
                            Padding="16,6"/>
                </Grid>

                <!-- File info (after selection) -->
                <TextBlock Margin="0,8,0,0"
                           FontSize="11"
                           Foreground="{DynamicResource TextSecondaryBrush}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasFileSelected}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                    <Run Text="Selected: "/>
                    <Run Text="{Binding SelectedFilePath, Mode=OneWay}" FontWeight="SemiBold"/>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- Main Content Area -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Step 2: Mapping Selection -->
            <GroupBox Grid.Row="0" Header="Step 2: Select Mapping Configuration" Margin="0,0,0,12">
                <StackPanel Margin="8">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                 Text="{Binding SelectedMappingPath, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="True"
                                 Background="{DynamicResource ControlBackgroundBrush}"
                                 Padding="8,6"
                                 Margin="0,0,8,0"
                                 VerticalContentAlignment="Center"/>

                        <Button Grid.Column="1"
                                Content="Browse..."
                                Command="{Binding BrowseMappingCommand}"
                                Style="{StaticResource BaseButtonStyle}"
                                Padding="16,6"/>
                    </Grid>

                    <!-- Mapping info -->
                    <TextBlock Margin="0,8,0,0"
                               FontSize="11"
                               Foreground="{DynamicResource TextSecondaryBrush}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasMappingSelected}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                        <Run Text="Selected: "/>
                        <Run Text="{Binding SelectedMappingPath, Mode=OneWay}" FontWeight="SemiBold"/>
                    </TextBlock>
                </StackPanel>
            </GroupBox>

            <!-- Step 3: Preview &amp; Options -->
            <GroupBox Grid.Row="1" Header="Step 3: Preview &amp; Options">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8">
                        <!-- Audit Status -->
                        <TextBlock FontSize="12"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Margin="0,0,0,12">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAuditing}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="? "/>
                            <Run Text="{Binding StatusMessage, Mode=OneWay}"/>
                        </TextBlock>

                        <!-- Audit Results -->
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasAuditResult}" Value="True">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>

                            <!-- Summary -->
                            <TextBlock FontSize="12"
                                       FontWeight="SemiBold"
                                       Margin="0,0,0,8">
                                <Run Text="File Preview: "/>
                                <Run Text="{Binding AuditResult.Summary, Mode=OneWay}"/>
                            </TextBlock>

                            <!-- Data Types -->
                            <TextBlock FontSize="11"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Margin="0,0,0,4">
                                <Run Text="Data Types: "/>
                                <Run Text="{Binding AuditResult.DetectedDataTypes.Count, Mode=OneWay}" FontWeight="SemiBold"/>
                            </TextBlock>

                            <!-- Scenarios (if any) -->
                            <StackPanel Margin="0,0,0,12">
                                <StackPanel.Style>
                                    <Style TargetType="StackPanel">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AuditResult.HasScenarios}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </StackPanel.Style>

                                <TextBlock FontSize="11"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Margin="0,0,0,4">
                                    <Run Text="Scenarios: "/>
                                    <Run Text="{Binding AuditResult.DiscoveredScenarios.Count, Mode=OneWay}" FontWeight="SemiBold"/>
                                </TextBlock>

                                <Button Content="Select Scenarios..."
                                        Command="{Binding SelectScenariosCommand}"
                                        Style="{StaticResource BaseButtonStyle}"
                                        Padding="12,6"
                                        HorizontalAlignment="Left"
                                        Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- Warnings -->
                            <ItemsControl ItemsSource="{Binding AuditResult.Warnings}"
                                          Margin="0,0,0,8">
                                <ItemsControl.Style>
                                    <Style TargetType="ItemsControl">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AuditResult.Warnings.Count, Converter={StaticResource GreaterThanZeroConverter}}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock FontSize="11"
                                                   Foreground="#FFA500"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,2">
                                            <Run Text="? "/>
                                            <Run Text="{Binding Mode=OneWay}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <!-- Errors -->
                            <ItemsControl ItemsSource="{Binding AuditResult.Errors}">
                                <ItemsControl.Style>
                                    <Style TargetType="ItemsControl">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding AuditResult.Errors.Count, Converter={StaticResource GreaterThanZeroConverter}}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ItemsControl.Style>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock FontSize="11"
                                                   Foreground="#FF4444"
                                                   FontWeight="SemiBold"
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,2">
                                            <Run Text="? "/>
                                            <Run Text="{Binding Mode=OneWay}"/>
                                        </TextBlock>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>

                        <!-- Empty state -->
                        <TextBlock FontSize="12"
                                   Foreground="{DynamicResource TextTertiaryBrush}"
                                   FontStyle="Italic"
                                   Margin="0,20,0,0">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding HasAuditResult}" Value="False"/>
                                                <Condition Binding="{Binding IsAuditing}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                            <Run Text="Select a file and mapping to preview import details."/>
                        </TextBlock>
                    </StackPanel>
                </ScrollViewer>
            </GroupBox>

            <!-- Step 4: Import Target -->
            <GroupBox Grid.Row="2" Header="Step 4: Import Target" Margin="0,12,0,0">
                <StackPanel Margin="8">
                    <RadioButton Content="Import to New Data"
                                 IsChecked="{Binding ImportToNewData}"
                                 Margin="0,0,0,4"/>
                    <RadioButton Content="Import to Old Data"
                                 IsChecked="{Binding ImportToNewData, Converter={StaticResource InverseBooleanConverter}}"
                                 Margin="0,4,0,0"/>
                </StackPanel>
            </GroupBox>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="3"
                Background="{DynamicResource SecondaryBackgroundBrush}"
                BorderBrush="{DynamicResource ControlBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,8"
                Margin="-20,12,-20,0">
            <TextBlock Text="{Binding StatusMessage}"
                       FontSize="11"
                       Foreground="{DynamicResource TextSecondaryBrush}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusMessage, Converter={StaticResource NullToVisibilityConverter}}" Value="Visible">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Import"
                    IsDefault="True"
                    Click="ImportButton_Click"
                    IsEnabled="{Binding CanProceed}"
                    Style="{StaticResource AccentButtonStyle}"
                    Padding="24,8"
                    Margin="0,0,8,0"
                    MinWidth="100"/>
            
            <Button Content="Cancel"
                    IsCancel="True"
                    Style="{StaticResource BaseButtonStyle}"
                    Padding="24,8"
                    MinWidth="100"/>
        </StackPanel>
    </Grid>
</Window>
