<Window x:Class="EasyAF.Modules.Project.Views.CompositeImportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:EasyAF.Modules.Project.ViewModels"
        Title="Import Data - Composite Mode"
        Width="800" Height="600"
        MinWidth="600" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}">

    <Window.Resources>
        <!-- BooleanToVisibilityConverter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Error TextBox Style -->
        <Style x:Key="ErrorTextBoxStyle" TargetType="TextBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasError}" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Error ComboBox Style -->
        <Style x:Key="ErrorComboBoxStyle" TargetType="ComboBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding HasError}" Value="True">
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Scrollable Content -->
        <ScrollViewer Grid.Row="0" 
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding FileGroups}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource BorderBrush}" 
                                BorderThickness="1" 
                                Margin="0,0,0,12"
                                Background="{DynamicResource ControlBackgroundBrush}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- File Header -->
                                <Border Grid.Row="0" 
                                        Background="{DynamicResource HeaderBackgroundBrush}" 
                                        Padding="8,6">
                                    <TextBlock Text="{Binding FileHeader}" 
                                               FontWeight="SemiBold"
                                               FontSize="13"
                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding FilePath}"/>
                                </Border>

                                <!-- Table Header (only show if file has scenarios) -->
                                <Grid Grid.Row="1" Visibility="{Binding IsNonScenarioFile, Converter={StaticResource BooleanToVisibilityConverter}}">
                                    <Border Padding="12,8">
                                        <TextBlock Text="{Binding NonScenarioMessage}" 
                                                   FontStyle="Italic"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"/>
                                    </Border>
                                </Grid>

                                <Grid Grid.Row="1">
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Setter Property="Visibility" Value="Visible"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsNonScenarioFile}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>
                                    
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*" MinWidth="150"/>
                                        <ColumnDefinition Width="2*" MinWidth="150"/>
                                        <ColumnDefinition Width="3*" MinWidth="200"/>
                                    </Grid.ColumnDefinitions>

                                    <Border Grid.Column="0" 
                                            Background="{DynamicResource SubHeaderBackgroundBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" 
                                            BorderThickness="0,1,1,1" 
                                            Padding="8,4">
                                        <TextBlock Text="Scenario" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Border>

                                    <Border Grid.Column="1" 
                                            Background="{DynamicResource SubHeaderBackgroundBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" 
                                            BorderThickness="0,1,1,1" 
                                            Padding="8,4">
                                        <TextBlock Text="Action" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Border>

                                    <Border Grid.Column="2" 
                                            Background="{DynamicResource SubHeaderBackgroundBrush}" 
                                            BorderBrush="{DynamicResource BorderBrush}" 
                                            BorderThickness="0,1,0,1" 
                                            Padding="8,4">
                                        <TextBlock Text="Target" 
                                                   FontWeight="SemiBold" 
                                                   Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </Border>
                                </Grid>

                                <!-- Scenario Rows -->
                                <ItemsControl Grid.Row="2" 
                                              ItemsSource="{Binding Scenarios}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="2*" MinWidth="150"/>
                                                    <ColumnDefinition Width="2*" MinWidth="150"/>
                                                    <ColumnDefinition Width="3*" MinWidth="200"/>
                                                </Grid.ColumnDefinitions>

                                                <!-- Scenario Name -->
                                                <Border Grid.Column="0" 
                                                        BorderBrush="{DynamicResource BorderBrush}" 
                                                        BorderThickness="0,0,1,1" 
                                                        Padding="8,6">
                                                    <TextBlock Text="{Binding ScenarioName}" 
                                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                                               VerticalAlignment="Center"
                                                               Margin="12,0,0,0"/>
                                                </Border>

                                                <!-- Action Dropdown -->
                                                <Border Grid.Column="1" 
                                                        BorderBrush="{DynamicResource BorderBrush}" 
                                                        BorderThickness="0,0,1,1" 
                                                        Padding="8,6">
                                                    <ComboBox SelectedValue="{Binding Action}"
                                                              SelectedValuePath="Tag"
                                                              VerticalAlignment="Center">
                                                        <ComboBoxItem Content="Add New" Tag="{x:Static vm:ImportAction.AddNew}"/>
                                                        <ComboBoxItem Content="Overwrite Existing" Tag="{x:Static vm:ImportAction.Overwrite}"/>
                                                        <ComboBoxItem Content="Do Not Import" Tag="{x:Static vm:ImportAction.DoNotImport}"/>
                                                    </ComboBox>
                                                </Border>

                                                <!-- Target Column -->
                                                <Border Grid.Column="2" 
                                                        BorderBrush="{DynamicResource BorderBrush}" 
                                                        BorderThickness="0,0,0,1" 
                                                        Padding="8,6">
                                                    <Grid>
                                                        <!-- TextBox for Add New -->
                                                        <TextBox Text="{Binding NewScenarioName, UpdateSourceTrigger=PropertyChanged}" 
                                                                 Visibility="{Binding ShowTextBox, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                 Style="{StaticResource ErrorTextBoxStyle}"
                                                                 Background="{DynamicResource ControlBackgroundBrush}"
                                                                 Foreground="{DynamicResource TextPrimaryBrush}"
                                                                 BorderBrush="{DynamicResource ControlBorderBrush}"
                                                                 VerticalAlignment="Center"/>
                                                        
                                                        <!-- ComboBox for Overwrite -->
                                                        <ComboBox ItemsSource="{Binding ExistingScenarios}"
                                                                  SelectedItem="{Binding SelectedExistingScenario}"
                                                                  Visibility="{Binding ShowComboBox, Converter={StaticResource BooleanToVisibilityConverter}}"
                                                                  Style="{StaticResource ErrorComboBoxStyle}"
                                                                  Background="{DynamicResource ControlBackgroundBrush}"
                                                                  Foreground="{DynamicResource TextPrimaryBrush}"
                                                                  BorderBrush="{DynamicResource ControlBorderBrush}"
                                                                  VerticalAlignment="Center"/>
                                                    </Grid>
                                                </Border>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Bottom Buttons -->
        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,12,0,0">
            <Button Content="Import Selected" 
                    Command="{Binding ImportCommand}"
                    Width="120" 
                    Height="32" 
                    Margin="0,0,8,0"
                    IsDefault="True"/>
            <Button Content="Cancel" 
                    Command="{Binding CancelCommand}"
                    Width="80" 
                    Height="32"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
