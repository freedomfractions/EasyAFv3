<UserControl x:Class="EasyAF.Modules.Project.Views.Panels.ProjectReportSettingsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <GroupBox Header="Report"
              Margin="0,0,22,0"
              HorizontalAlignment="Stretch"
              BorderBrush="{DynamicResource ControlBorderBrush}"
              Foreground="{DynamicResource TextPrimaryBrush}">
        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="140"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/>
                <RowDefinition Height="32"/>
                <RowDefinition Height="32"/>
            </Grid.RowDefinitions>
            
            <!-- Spec Path -->
            <TextBlock Grid.Row="0" Grid.Column="0" Text="Spec:" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding SpecPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0" HorizontalAlignment="Stretch"/>
            <Button Grid.Row="0" Grid.Column="2" Content="Browse..." Command="{Binding BrowseSpecCommand}" Padding="8,4" Style="{StaticResource BaseButtonStyle}"/>
            
            <!-- Template Path -->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Template:" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TemplatePath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0" HorizontalAlignment="Stretch"/>
            <Button Grid.Row="1" Grid.Column="2" Content="Browse..." Command="{Binding BrowseTemplateCommand}" Padding="8,4" Style="{StaticResource BaseButtonStyle}"/>
            
            <!-- Output Folder -->
            <TextBlock Grid.Row="2" Grid.Column="0" Text="Output Folder:" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding OutputPath, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Margin="0,0,8,0" HorizontalAlignment="Stretch"/>
            <Button Grid.Row="2" Grid.Column="2" Content="Browse..." Command="{Binding BrowseOutputCommand}" Padding="8,4" Style="{StaticResource BaseButtonStyle}"/>
        </Grid>
    </GroupBox>
</UserControl>
