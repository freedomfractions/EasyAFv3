<UserControl x:Class="EasyAF.Modules.Map.Views.MapModuleSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource SecondaryBackgroundBrush}">
    
    <!-- DEPRECATED: 2025-11-28 Data Types moved to global Shell settings -->
    <!-- Modified for: This view now only contains Maps Directory setting -->
    <!-- The Data Types configuration has been moved to Shell > Options > Data Types tab -->
    <!-- TODO: Remove data type-related code from MapModuleSettingsViewModel -->
    
    <Grid Margin="20">
        <StackPanel>
            
            <!-- Header -->
            <TextBlock Text="Map Editor Configuration"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            
            <!-- Description -->
            <TextBlock TextWrapping="Wrap"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       Margin="0,0,0,20">
                <Run Text="Configure Map Editor-specific settings."/>
                <LineBreak/>
                <Run Text="Note: Data type and property filtering has been moved to the global 'Data Types' tab."/>
            </TextBlock>
            
            <!-- Maps Directory Section -->
            <TextBlock Text="Maps Directory"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,4"/>
            <TextBlock Text="Default location for .ezmap mapping configuration files"
                       Foreground="{DynamicResource TextSecondaryBrush}"
                       FontSize="11"
                       Margin="0,0,0,8"/>
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0"
                         Text="{Binding MapsDirectory, UpdateSourceTrigger=PropertyChanged}"
                         VerticalContentAlignment="Center"
                         Height="32"
                         Margin="0,0,8,0"
                         Background="{DynamicResource ControlBackgroundBrush}"
                         Foreground="{DynamicResource TextPrimaryBrush}"
                         BorderBrush="{DynamicResource ControlBorderBrush}"/>
                <Button Grid.Column="1"
                        Content="Browse..."
                        Width="80"
                        Height="32"
                        Command="{Binding BrowseMapsDirectoryCommand}"
                        Style="{StaticResource BaseButtonStyle}"/>
            </Grid>
            
            <!-- DEPRECATED: Data Types section moved to Shell > Options > Data Types tab -->
            <!-- The code below is hidden but kept for reference during migration -->
            <!-- TODO: Remove this entire section and related ViewModel code -->
            <!--
            <Grid Margin="0,0,0,8" Visibility="Collapsed">
                <TextBlock Text="Data Types &amp; Properties (DEPRECATED - use global Data Types tab)"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           VerticalAlignment="Center"/>
            </Grid>
            -->
            
        </StackPanel>
    </Grid>
</UserControl>
