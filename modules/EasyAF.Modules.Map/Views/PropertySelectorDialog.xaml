<Window x:Class="EasyAF.Modules.Map.Views.PropertySelectorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DataTypeDisplayName, StringFormat='Configure {0} Properties'}"
        Width="500" Height="600"
        MinWidth="400" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{DynamicResource WindowBackgroundBrush}">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header & Search -->
            <RowDefinition Height="Auto"/> <!-- Toolbar -->
            <RowDefinition Height="*"/>    <!-- Property List -->
            <RowDefinition Height="Auto"/> <!-- Count -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>
        
        <!-- Header & Search -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Header -->
            <TextBlock Grid.Row="0"
                       Text="Select which properties should be available for mapping:"
                       FontSize="13"
                       Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,8"/>
            
            <!-- Search Box -->
            <Border Grid.Row="1"
                    Background="{DynamicResource ControlBackgroundBrush}"
                    BorderBrush="{DynamicResource ControlBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Search Icon -->
                    <TextBlock Grid.Column="0"
                               Text="&#xE721;"
                               FontFamily="Segoe MDL2 Assets"
                               FontSize="14"
                               Margin="8,0,4,0"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextSecondaryBrush}"/>
                    
                    <!-- Search TextBox -->
                    <TextBox Grid.Column="1"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             BorderThickness="0"
                             Background="Transparent"
                             VerticalAlignment="Center"
                             Padding="0,6">
                        <TextBox.Style>
                            <Style TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Tag" Value="Search properties..."/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </TextBox.Style>
                    </TextBox>
                </Grid>
            </Border>
        </Grid>
        
        <!-- Toolbar -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <Button Content="Select All"
                    Command="{Binding SelectAllCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Padding="8,4"
                    Margin="0,0,8,0"/>
            <Button Content="Select None"
                    Command="{Binding SelectNoneCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Padding="8,4"
                    Margin="0,0,8,0"/>
            <Button Content="Reset to Defaults"
                    Command="{Binding ResetToDefaultsCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    Padding="8,4"/>
        </StackPanel>
        
        <!-- Property List -->
        <Border Grid.Row="2"
                BorderBrush="{DynamicResource ControlBorderBrush}"
                BorderThickness="1"
                Background="{DynamicResource ControlBackgroundBrush}">
            <ListBox ItemsSource="{Binding PropertiesView}"
                     BorderThickness="0"
                     Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <CheckBox IsChecked="{Binding IsEnabled}"
                                  Margin="4,4"
                                  Foreground="{DynamicResource TextPrimaryBrush}">
                            <CheckBox.Content>
                                <StackPanel Margin="4,0">
                                    <!-- CROSS-MODULE EDIT: 2025-01-18 Property Selector Friendly Names -->
                                    <!-- Modified for: Display friendly Description first, PropertyName second -->
                                    <!-- Related modules: Map (PropertyDiscoveryService extracts Description from [Description] attribute) -->
                                    <!-- Rollback instructions: Swap order back (PropertyName first, Description second) -->
                                    
                                    <!-- Primary Display: Description (friendly name) -->
                                    <TextBlock FontWeight="SemiBold"
                                               Foreground="{DynamicResource TextPrimaryBrush}">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <!-- Default: show Description -->
                                                <Setter Property="Text" Value="{Binding Description}"/>
                                                <Style.Triggers>
                                                    <!-- Fallback to PropertyName if Description is missing -->
                                                    <DataTrigger Binding="{Binding Description}" Value="{x:Null}">
                                                        <Setter Property="Text" Value="{Binding PropertyName}"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Description}" Value="">
                                                        <Setter Property="Text" Value="{Binding PropertyName}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                    
                                    <!-- Secondary Display: PropertyName (C# property name) - only if Description exists -->
                                    <TextBlock Text="{Binding PropertyName}"
                                               FontSize="10"
                                               Foreground="{DynamicResource TextSecondaryBrush}"
                                               Margin="0,2,0,0"
                                               Visibility="{Binding HasDescription, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </CheckBox.Content>
                        </CheckBox>
                    </DataTemplate>
                </ListBox.ItemTemplate>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="4,2">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource HoverBackgroundBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </ListBox.ItemContainerStyle>
            </ListBox>
        </Border>
        
        <!-- Property Count -->
        <TextBlock Grid.Row="3"
                   Margin="0,8,0,12"
                   Foreground="{DynamicResource TextSecondaryBrush}"
                   FontSize="12">
            <Run Text="{Binding EnabledCount, Mode=OneWay}"/>
            <Run Text="of"/>
            <Run Text="{Binding TotalCount, Mode=OneWay}"/>
            <Run Text="properties enabled"/>
        </TextBlock>
        
        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="OK"
                    Command="{Binding OkCommand}"
                    Style="{StaticResource AccentButtonStyle}"
                    IsDefault="True"
                    MinWidth="80"
                    Padding="16,6"
                    Margin="0,0,8,0"/>
            <Button Content="Cancel"
                    Command="{Binding CancelCommand}"
                    Style="{StaticResource BaseButtonStyle}"
                    IsCancel="True"
                    MinWidth="80"
                    Padding="16,6"/>
        </StackPanel>
    </Grid>
</Window>
